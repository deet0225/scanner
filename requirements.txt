fastapi>=0.110.0
uvicorn[standard]>=0.29.0
gunicorn>=21.2.0
yfinance>=0.2.51
pandas>=2.0.0
numpy>=1.24.0
requests>=2.31.0
urllib3>=2.0.0
apscheduler>=3.10.0
pytz>=2024.1
ta>=0.11.0                  # pure-Python TA library (replaces pandas_ta — supports Python 3.14+)
# ── Additional data sources for improved accuracy ──────────────────────────
nsepython>=2.7              # NSE India live + historical (direct API, authoritative)
apify-client>=1.7.0         # Apify actor runner  (screener.in Apify actor, optional)
# NOTE: tvDatafeed is NOT on PyPI; must be installed from GitHub:
#   pip install git+https://github.com/StreamAlpha/tvdatafeed.git
# It will be silently skipped if unavailable (TradingViewClient handles ImportError).
